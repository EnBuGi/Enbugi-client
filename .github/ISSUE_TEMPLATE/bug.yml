name: "🐛 Bug"
description: "버그 리포트"
title: "[BUG] "
labels: ["bug"]
body:
  - type: dropdown
    id: area
    attributes:
      label: "영역(Area)"
      options:
        - Frontend
        - Backend
        - Common
        - DevOps/Infra
    validations:
      required: true

  - type: textarea
    id: what_happened
    attributes:
      label: "현상(What happened)"
      description: "무슨 문제가 발생했나요?"
      placeholder: "예) 로그인 시 CORS 에러로 요청이 차단됨"
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: "기대 결과(Expected)"
      placeholder: "예) 로그인 요청이 정상 처리되고 토큰/세션이 저장되어야 함"
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: "재현 방법(Steps to reproduce)"
      description: "최대한 구체적으로"
      placeholder: |
        1) ...
        2) ...
        3) ...
    validations:
      required: true

  - type: dropdown
    id: frequency
    attributes:
      label: "재현 빈도(Frequency)"
      options:
        - Always (100%)
        - Often
        - Sometimes
        - Rare
        - Unable to reproduce
    validations:
      required: true

  - type: dropdown
    id: severity
    attributes:
      label: "심각도(Severity)"
      description: "서비스 영향도"
      options:
        - S1 - Blocker (서비스 불가/치명)
        - S2 - Major (주요 기능 장애)
        - S3 - Minor (부분 장애/우회 가능)
        - S4 - Trivial (경미/개선)
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: "환경(Environment)"
      description: "가능한 정보만 적기"
      placeholder: |
        - App/Service: (FE/BE/Batch 등)
        - OS:
        - Browser/Device:
        - Version/Commit:
        - API URL:
    validations:
      required: false

  - type: textarea
    id: logs
    attributes:
      label: "로그/스크린샷(Logs / Screenshots)"
      description: "에러 로그, 네트워크 탭, 콘솔 로그 등"
      placeholder: |
        ```text
        paste logs here
        ```
    validations:
      required: false

  - type: textarea
    id: root_cause
    attributes:
      label: "원인 추정/분석(Optional)"
      placeholder: "- 추정 원인\n- 관련 코드/PR/이슈 링크"
    validations:
      required: false

  - type: textarea
    id: fix_plan
    attributes:
      label: "해결 방향(Fix plan)"
      placeholder: "- [ ] 조치 1\n- [ ] 조치 2\n- [ ] 테스트/검증"
    validations:
      required: true
